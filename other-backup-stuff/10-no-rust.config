# Rust support requires bindgen, which invokes clang. clang doesn't load
# the GCC latent_entropy plugin, so the __latent_entropy attribute used in
# include/linux/random.h shows up as undeclared and bindgen aborts. The
# hardened profile enables both, which can't coexist — drop Rust.
# CONFIG_RUST is not set
