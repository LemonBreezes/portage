# Build the binary versions of these packages instead.
dev-util/shellcheck
sys-kernel/gentoo-kernel
sys-kernel/gentoo-sources
sys-kernel/genkernel
app-text/pandoc
dev-java/icedtea
dev-java/openjdk
www-client/firefox
dev-lang/rust

# Compile these from source instead.
app-office/libreoffice-bin

# Some overlay package versions do not build.
dev-lang/python::pg_overlay
app-office/libreoffice::pg_overlay
app-office/libreoffice-l10n::pg_overlay
dev-util/shfmt::nest
app-portage/genlop::src_prepare-overlay

# Causing other builds to fail.
gnome-base/librsvg::pg_overlay

# This version fails to build or install.
=app-shells/fish-4.0.1

# The IGC Emacs is broken right now.
app-editors/emacs::cae

# Pahole BTF encoding fails on nvidia-modeset.ko with current kernel.
# Re-enable once the kernel is built without CONFIG_DEBUG_INFO_BTF_MODULES,
# or upstream pahole/nvidia handle the unsupported DW_TAG_reference_type.
x11-drivers/nvidia-drivers

# Hold the kernel at the 6.19 series: sys-fs/zfs-2.4.x caps
# MODULES_KERNEL_MAX at 6.19, so building zfs against kernel 7.x dies in
# pkg_setup ("Linux 6.19 is the latest supported version"). Drop the
# mask when sys-fs/zfs ships an ebuild that supports kernel 7.x.
>=sys-kernel/gentoo-kernel-bin-7
>=sys-kernel/gentoo-kernel-7
>=sys-kernel/gentoo-sources-7
>=sys-kernel/vanilla-kernel-7
>=virtual/dist-kernel-7
